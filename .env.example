# NextOffer configuration lives in two files:
#   frontend/.env.example  ->  frontend/.env.local
#   backend/.env.example   ->  backend/.env
#
# AUTH_SECRET must be identical in both. Never commit real secrets.
